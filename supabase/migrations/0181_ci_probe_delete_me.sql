-- TEMPORARY PROBE — never merged, never applied anywhere.
-- Exists to prove the pull-request path of tools/ci/migration-notice.mjs works,
-- because until now that branch had only ever run on `push`.
select 1;
